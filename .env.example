# Flask secret key used for signing sessions. Generate a long random string for production.
SECRET_KEY=change-this-secret-key-in-production

# Default admin account (created automatically on first run if it doesn't exist).
# CHANGE THESE before deploying anywhere public.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
